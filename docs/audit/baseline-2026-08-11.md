# Auditoría baseline — 11 agosto 2026

Diagnóstico del estado real del repositorio antes de empezar la refactorización.
**Este documento no propone soluciones**: sólo mide y describe lo que hay.

- Rama: `develop` (commit `c973e81`)
- Gestor de paquetes hoy: **npm** (la migración a pnpm todavía no está hecha)
- Next.js **16.2.9** con Turbopack, React 19.2.3, Node 24.15.0
- Producción verificada: `https://adriana-portfolio-blue.vercel.app`

Método: lectura completa de `src/`, `npm run build`, inspección del HTML
prerenderizado y de los chunks en `.next/`, y `curl -I` contra producción.
Todo número de este documento procede de una de esas fuentes.

---

## 0. Resumen de lo más grave

| # | Hallazgo | Evidencia |
|---|---|---|
| 1 | **La página se sirve sin contenido**: el HTML prerenderizado tiene 0 caracteres de texto visible | `.next/server/app/index.html` → `<body>` = `<div hidden></div>` |
| 2 | First Load JS de **302 KB gzip / 1 059 KB sin comprimir** para una landing de una sola ruta | suma de los 10 chunks referenciados por `index.html` |
| 3 | `flag-icons` mete **271 banderas (32 KB, 44 % del CSS)** para usar 2 | `.next/static/chunks/3uu7-rv2scfyb.css` |
| 4 | El endpoint de contacto **vuelca el body completo a los logs** | `src/app/api/contact/route.ts:16` |
| 5 | Sin sitemap, robots, Open Graph, JSON-LD, canonical ni hreflang | no existe ninguno de los ficheros/APIs |
| 6 | `framer-motion` declarado y **jamás importado**; se anuncia como stack en README, portfolio y skills | `grep` sin resultados en `src/` |

---

## 1. Medición del build

`npm run build` — exit 0, sin errores ni warnings.

```
▲ Next.js 16.2.9 (Turbopack)
✓ Compiled successfully in 8.0s
  Finished TypeScript in 7.2s
✓ Generating static pages (4/4) in 762ms

Route (app)
┌ ○ /
├ ○ /_not-found
└ ƒ /api/contact
```

**Aviso metodológico:** Next 16 con Turbopack **ya no imprime la tabla de
tamaños por ruta ni el First Load JS**. La salida de arriba es todo lo que da.
Los números siguientes los he calculado a mano: he leído los `<script>` que
referencia el HTML prerenderizado de `/` y he medido y comprimido cada chunk.

### First Load JS de `/`

| Chunk | Sin comprimir | Gzip |
|---|---:|---:|
| `3-kyd-ctkvmev.js` | 374,6 KB | 96,9 KB |
| `3peubv2924kx4.js` | 222,0 KB | 69,1 KB |
| `07u_h6i539_wj.js` | 146,8 KB | 39,4 KB |
| `0cz1d0mv5g_q7.js` (polyfills) | 110,0 KB | 38,5 KB |
| `14mrh2-p_w84d.js` | 53,4 KB | 12,5 KB |
| `0i64ppjut-x7c.js` | 53,2 KB | 17,6 KB |
| `15orcrkp-_9ct.js` | 49,5 KB | 10,3 KB |
| `0rf23obzcp0nl.js` | 22,3 KB | 8,1 KB |
| `3-5k80pt3ds20.js` | 17,3 KB | 6,0 KB |
| `turbopack-36wsln1gez59f.js` | 10,3 KB | 4,1 KB |
| **TOTAL JS** | **1 059,4 KB** | **302,3 KB** |
| CSS `3uu7-rv2scfyb.css` | 73,1 KB | 16,7 KB |
| HTML `/` | 9,9 KB | — |

Las tres rutas del build son `/`, `/_not-found` y `/api/contact`. Sólo `/`
tiene contenido propio.

### Qué hay dentro de los chunks

Los chunks van minificados y sin source maps (`find .next/static -name "*.map"`
→ 0 resultados), así que no puedo atribuir bytes por paquete con precisión.
Lo que sí puedo medir es el número de módulos por paquete en el grafo de build
(`.next/diagnostics/analyze/data/modules.data`):

| Paquete | Módulos |
|---|---:|
| `next` | 1704 |
| **`flag-icons`** | **1084** |
| **`zod`** | **624** |
| `lucide-react` | 176 |
| `react-i18next` | 88 |
| `postal-mime`, `entities`, `domutils`, `htmlparser2`, `prettier` | 20/18/14/6/8 |

Los cinco últimos vienen de `react-email` y son sólo de servidor (ruta de API),
no llegan al cliente.

**No puedo afirmar** cuántos KB exactos aporta cada librería al bundle de
cliente sin source maps o un analizador. Lo dejo como pendiente de medir.

---

## 2. El hallazgo principal: la página se sirve vacía

`I18nProvider` envuelve todo el árbol y devuelve `null` mientras no se haya
inicializado, y sólo se inicializa dentro de un `useEffect` (cliente):

```tsx
// src/components/providers/I18nProvider.tsx:25-27
if (!isInitialized) {
  return null;
}
```

`layout.tsx` lo pone por encima de `ThemeProvider` y de `children`, así que en
render de servidor **el árbol entero devuelve `null`**.

Verificado sobre el HTML generado:

```
HTML total:            9 891 bytes
<body> :               8 188 bytes
Texto visible:         0 caracteres
Markup real del body:  <div hidden=""><!--$--><!--/$--></div>
```

Las dos apariciones de «Adriana Suárez» en el fichero están en `<title>` y en
`<meta name="description">`, no en el cuerpo.

Y no es un artefacto local — producción sirve exactamente el mismo shell:

```
$ curl -I https://adriana-portfolio-blue.vercel.app
HTTP/1.1 200 OK
Content-Length: 9893      <-- coincide con el build local
X-Nextjs-Prerender: 1
```

Consecuencias medibles: la ruta está marcada como estática (`○`) pero no
prerenderiza nada; todo el contenido depende de descargar y ejecutar 302 KB de
JS; el LCP no puede empezar hasta que i18next termine de inicializarse en el
cliente.

---

## 3. Inventario de `"use client"`

**14 de 20 componentes** son de cliente. Sólo `page.tsx` y 5 primitivas de `ui/`
son de servidor — y `page.tsx` no llega a renderizar nada útil por lo del
apartado 2.

| Archivo | Qué usa realmente | ¿Justificado? |
|---|---|---|
| `providers/I18nProvider.tsx` | `useState`, `useEffect`, `useRef` | Sí — inicializa i18next en cliente |
| `layout/ThemeProvider.tsx` | wrapper de `next-themes` | Sí |
| `layout/ThemeToggle.tsx` | `useTheme`, `useState`, `useEffect`, `onClick` | Sí |
| `layout/LanguageToggle.tsx` | `useState`, `useRef`, `useEffect` (click-outside), `onClick` | Sí |
| `layout/Header.tsx` | `useState`+`useEffect` (scroll), `onClick`, `useTranslation` | Sí, pero por el listener de scroll y el CV; el `<nav>` y el logo no lo necesitan |
| `sections/Hero/Terminal.tsx` | 4 `useState`, 3 `useRef`, 3 `useEffect`, form | Sí — es un REPL |
| `ui/TypingText.tsx` | `useState`, `useEffect` (animación letra a letra) | Sí |
| `sections/Contact/Contact.tsx` | `useState`, `react-hook-form`, `fetch` | Sí |
| `sections/Hero/Hero.tsx` | `useTranslation`, 2 `onClick` (descargar CV, scroll) | **Sólo por `useTranslation` y 2 handlers**; el texto es estático |
| `sections/About/About.tsx` | `useTranslation` | **Sólo por `useTranslation`** — no hay estado, ni efectos, ni handlers |
| `sections/Experience/Experience.tsx` | `useTranslation` vía `useExperience` | **Sólo por `useTranslation`** |
| `sections/Skills/Skills.tsx` | `useTranslation` vía `useSkills` | **Sólo por `useTranslation`** |
| `sections/Projects/Projects.tsx` | `useTranslation` vía `useProjects` | **Sólo por `useTranslation`** |
| `layout/Footer.tsx` | `useTranslation`, `new Date()` | **Sólo por `useTranslation`** |

**6 componentes (About, Experience, Skills, Projects, Footer y en gran medida
Hero) son de cliente exclusivamente porque leen traducciones con
`useTranslation`.** No tienen estado, ni efectos, ni handlers propios más allá
de dos botones en Hero. Es i18n en cliente lo que arrastra al cliente el 100 %
del contenido de la página.

### Caso aparte: `ProjectCard.tsx`

No lleva `"use client"` pero llama a `useTranslation()`
(`src/components/sections/Projects/ProjectCard.tsx:12`). Funciona sólo porque su
único consumidor (`Projects.tsx`) sí es de cliente y lo arrastra al grafo de
cliente. Si algún día se renderizara desde un componente de servidor, rompería
en build. Es una dependencia implícita, no una decisión.

### Primitivas de servidor (correctas)

`ui/Button.tsx`, `ui/Card.tsx`, `ui/Section.tsx`, `ui/Badge.tsx` e
`icons/SocialIcons.tsx` no llevan `"use client"` y no lo necesitan.

---

## 4. `src/app/api/contact/route.ts` — qué se loguea exactamente

Cinco `console.log` bajo el comentario `// 🔍 LOGS DE DIAGNÓSTICO - BORRAR DESPUÉS`:

| Línea | Sentencia | Qué expone |
|---|---|---|
| 16 | `console.log("📦 Body received:", JSON.stringify(body, null, 2))` | **El body entero: nombre, email y mensaje completo del visitante** |
| 17 | `console.log("📦 Language type:", typeof body.language)` | tipo (inocuo) |
| 18 | `console.log("📦 Language value:", body.language)` | valor de idioma (inocuo) |
| 19 | `console.log("📦 Language === 'es':", ...)` | booleano (inocuo) |
| 20 | `console.log("📦 Language === 'en':", ...)` | booleano (inocuo) |

La línea 16 es la grave: vuelca datos personales completos a los logs de
Vercel en cada envío del formulario.

Además, en la línea 69:

```ts
console.error("Contact form error:", error);
```

Loguea el objeto de error completo. Si el fallo viene de Resend, ahí puede
acabar el contenido de la petición saliente. No he podido comprobar qué incluye
exactamente el error de Resend sin provocar un fallo real contra su API.

Y en la respuesta de éxito (líneas 61-67):

```ts
return NextResponse.json({
  message: "Message sent successfully",
  data: { notificationEmail, confirmationResponse },   // <-- respuesta cruda de Resend
}, { status: 200 });
```

Devuelve al cliente las respuestas crudas del proveedor (incluyen los `id` de
los emails enviados). El cliente no las usa para nada
(`Contact.tsx:47` sólo mira `response.ok`).

### Lo que sí está bien en este archivo

Comprobado y **descartado como problema**: la rama de error 400 sí funciona.
Verifiqué contra la versión instalada (zod 4.3.6) que `ZodError` cumple
`error instanceof Error && error.name === "ZodError"` → `true`. La validación
con Zod se ejecuta antes de tocar los datos (línea 24) y los mensajes de error
genéricos al cliente son correctos.

### Lo que falta

- Sin rate limiting de ningún tipo.
- Sin validación de origen ni comprobación de `Content-Type`.
- Sin honeypot ni ninguna defensa anti-bot.
- Destinatario y remitente hardcodeados (`adsuarez09@gmail.com`,
  `onboarding@resend.dev`) en lugar de variables de entorno.

---

## 5. `layout.tsx` — `lang` y metadata

### `lang`

```tsx
// src/app/layout.tsx:33
<html lang="en" suppressHydrationWarning>
```

Fijo a `"en"`, hardcodeado. El sitio sirve español e inglés y el idioma se
decide en cliente (`localStorage` → `navigator` → `htmlTag`, ver
`src/i18n/config.ts:26`). Verificado en el HTML servido: `<html lang="en">`.

Nada actualiza el atributo cuando se cambia de idioma: `LanguageToggle` llama a
`i18n.changeLanguage()` y nada más. Es decir, con la web en español el
documento sigue declarando `lang="en"`.

### Metadata

Objeto `Metadata` estático (líneas 17-25). Sólo tiene:

- `title`: `"Adriana Suárez | Frontend & Fullstack Developer"`
- `description`: una frase en inglés
- `icons.icon` / `icons.shortcut`: `/web.png`

**No hay** `generateMetadata()`, ni `metadataBase`, ni `openGraph`, ni
`twitter`, ni `alternates`/`canonical`, ni `hreflang`, ni `robots`. Al ser
estática, la metadata no puede variar por idioma.

### Detalle suelto

Líneas 38-40: hay un comentario `{/* ← NUEVO */}` y un `{" "}` sueltos dentro
del JSX, restos de una edición anterior.

---

## 6. SEO: sitemap, robots, Open Graph, JSON-LD

Búsqueda de ficheros y de las APIs de metadata en todo `src/`:

| Elemento | Estado |
|---|---|
| `sitemap.ts` / `sitemap.xml` | **No existe** |
| `robots.ts` / `robots.txt` | **No existe** |
| `manifest.ts` / `manifest.json` | **No existe** |
| `opengraph-image` / `twitter-image` | **No existe** |
| `openGraph` en metadata | **No existe** |
| `twitter` en metadata | **No existe** |
| JSON-LD / `schema.org` / `application/ld+json` | **No existe** |
| `metadataBase` | **No existe** |
| `canonical` / `alternates` / `hreflang` | **No existe** |
| `apple-icon` | **No existe** |

Consecuencia comprobable: al compartir el enlace en LinkedIn, WhatsApp o
Twitter no hay imagen ni título de tarjeta, sólo la URL. Y como el body va
vacío (apartado 2), un crawler que no ejecute JS no ve absolutamente nada.

---

## 7. Strings de UI fuera de los ficheros de locales

### Estado de los locales — correcto

`es.json` y `en.json` tienen **101 claves cada uno, con paridad perfecta**: 0
claves huérfanas en cualquiera de los dos sentidos. Las únicas 2 cadenas
idénticas entre idiomas son nombres propios de proyectos («Café de Altura»,
«Los Inmaduros Rollers Madrid»), lo cual es correcto.

### Strings hardcodeados: 15

**7 textos visibles:**

| Archivo | Línea | Texto |
|---|---|---|
| `sections/Contact/Contact.tsx` | 99, 122, 145 | `Contact:` (×3) |
| `sections/Contact/Contact.tsx` | 100 | `Name` |
| `sections/Contact/Contact.tsx` | 123 | `Email` |
| `sections/Contact/Contact.tsx` | 146 | `Message` |
| `layout/Footer.tsx` | 58 | `Madrid, España` |

Los seis primeros son las etiquetas del formulario de contacto: siempre en
inglés, incluso con el sitio en español.

**8 atributos de accesibilidad, todos sólo en inglés:**

| Archivo | Línea | Atributo | Valor |
|---|---|---|---|
| `layout/LanguageToggle.tsx` | 45 | `aria-label` | `Select language (current: ${...})` |
| `layout/LanguageToggle.tsx` | 65 | `aria-label` | `Switch to ${lang.name}` |
| `layout/LanguageToggle.tsx` | 78 | `aria-label` | `${lang.name} flag` |
| `layout/ThemeToggle.tsx` | 28 | `aria-label` | `Toggle theme` |
| `sections/About/About.tsx` | 71, 79 | `alt` | `Adriana Suárez - Frontend Developer` (×2) |
| `sections/Projects/ProjectCard.tsx` | 50 | `aria-label` | `View on GitHub` |
| `sections/Projects/ProjectCard.tsx` | 62 | `aria-label` | `View live demo` |

Un usuario de lector de pantalla en español oye toda la navegación en inglés.

### No cuentan como violación

`NANA_DEV` (marca), `Adriana Suárez` (nombre propio), `Next.js 16` (producto),
`bash — adriana@portfolio` (cromo de la terminal) y la lista de comandos
`whoami / help / skills / projects / contact / clear` (son comandos, no copy).

---

## 8. Accesibilidad

- **El formulario de contacto no tiene ni un solo `<label>`.** No hay `htmlFor`
  ni `id` en ningún input de `Contact.tsx`. Las etiquetas visibles son `<span>`
  decorativos sin asociación programática. Los tres campos (nombre, email,
  mensaje) sólo se anuncian por su `placeholder`.
- Los errores de validación (`Contact.tsx:111`, `134`, `157`) se pintan como
  `<p>` sin `role="alert"` ni `aria-describedby`, así que no se anuncian.
- El estado de envío (enviando / éxito / error) cambia sólo el texto del botón,
  sin región `aria-live`.
- No hay `focus-visible` declarado en ningún sitio; los inputs llevan
  `focus:ring-0`, que **elimina** el anillo de foco por defecto.
- Sin `skip-link`.
- Lo que sí está bien: todas las imágenes tienen `alt`, los enlaces sociales
  tienen `aria-label`, los iconos decorativos llevan `aria-hidden`, y la
  jerarquía de encabezados es correcta (un `h1` en Header, `h2` por sección).

**No he ejecutado ninguna herramienta automática de accesibilidad** (axe,
Lighthouse). Lo de arriba sale de leer el código. Una auditoría con
herramienta está pendiente.

---

## 9. Dependencias y código muerto

| Elemento | Estado | Evidencia |
|---|---|---|
| **`framer-motion` (^12.29.0)** | **Declarado, nunca importado** | `grep -rn "framer-motion\|from \"motion" src/` → 0 resultados |
| `src/lib/animations.ts` | **Fichero vacío** (0 bytes) | — |
| `src/types/index.ts` | **Fichero vacío** (0 bytes) | contradice «shared types go in `src/types/`» |
| `src/lib/constants.ts` | **Nunca importado** | `grep -rn "@/lib/constants" src/` → 0 resultados |
| `public/images/avatar.jpg` | **Nunca referenciado** — 1,20 MB | — |
| `@react-email/render` + `react-email` | Usados sólo en la ruta de API | correcto, no llega al cliente |

### `constants.ts` además contiene datos incorrectos

Aunque no se use, si algún día se usa arrastra errores:

- `url: "https://adriana-portfolio.vercel.app"` — la URL real es
  `adriana-portfolio-blue.vercel.app`
- `linkedin: "www.linkedin.com/in/adriana-suarez-colmenares"` — sin protocolo y
  distinto del que usa `Footer.tsx:28`
  (`.../in/adriana-su%C3%A1rez-4562a5249/`)
- `author.name: "Adriana"` — incompleto

### `flag-icons`

`src/app/globals.css:2` importa la hoja completa:

```css
@import "flag-icons/css/flag-icons.min.css";
```

Resultado medido en el CSS final: **271 banderas definidas, 542 reglas `.fi-*`,
32,4 KB — el 44 % de los 73,1 KB del bundle CSS**. Se usan exactamente dos:
`fi-gb` y `fi-es` (`LanguageToggle.tsx:9-10`).

### Coherencia de lo que el portfolio dice de sí mismo

Esto importa porque lo leen reclutadores:

- `src/data/projects.ts:25` lista **«Framer Motion»** entre las tecnologías del
  propio portfolio. No se usa.
- `src/data/skills.ts:30` declara **«Framer Motion — 70 %»**.
- `README.md:15` y `:26` anuncian «Animations: Framer Motion».

---

## 10. Imágenes

| Archivo | Peso | Uso |
|---|---:|---|
| `public/images/avatar.jpg` | **1 197 KB** | **ninguno** |
| `public/images/nana_con_gafas.png` | 1 054 KB | About, `priority` |
| `public/images/nana_sin_gafas.png` | 1 048 KB | About, `priority` |
| `public/images/projects/los_inmaduros_roller_madrid.png` | 1 188 KB | ProjectCard |
| `public/images/projects/cafe_de_altura.png` | 1 037 KB | ProjectCard |
| `public/images/projects/adriana_portfolio.png` | 101 KB | ProjectCard |
| `public/web.png` | — | favicon |

Observaciones:

- **Ningún `<Image>` define `sizes`**, y los tres usan `fill`. Sin `sizes`,
  Next asume `100vw` y sirve la variante más grande aunque el contenedor mida
  320 px (`w-80` en About).
- About carga **dos** imágenes de ~1 MB con `priority`, ambas del mismo rostro,
  para un efecto hover que sólo existe en `lg:`. En móvil se descarga la de
  `hidden lg:block` igualmente, porque `hidden` es CSS y no impide la descarga.
- Los originales son PNG de ~1 MB para fotografías; ningún AVIF/WebP de origen.

No he medido el peso de las variantes optimizadas que sirve Vercel en
producción: haría falta cargar la página con red instrumentada. Pendiente.

---

## 11. Seguridad y cabeceras

`next.config.ts` está vacío:

```ts
const nextConfig: NextConfig = {
  /* config options here */
};
```

Sin `headers()`, sin CSP, sin `poweredByHeader: false`, sin configuración de
imágenes.

Verificado contra producción (`curl -I`, 11 ago 2026):

| Cabecera | Estado |
|---|---|
| `Strict-Transport-Security` | ✅ presente (por defecto de Vercel) |
| `Content-Security-Policy` | ❌ ausente |
| `X-Frame-Options` | ❌ ausente |
| `X-Content-Type-Options` | ❌ ausente |
| `Referrer-Policy` | ❌ ausente |
| `Permissions-Policy` | ❌ ausente |
| `Access-Control-Allow-Origin` | ⚠️ `*` sobre el HTML |

Sobre secretos: `.env.local` está en `.gitignore` (línea 34) y `.env.example`
sólo contiene la clave sin valor. Correcto. **No he abierto `.env.local`.**
Nota menor: `.gitignore` ignora `.env.local` pero no `.env` ni `.env*.local`.

---

## 12. Calidad, tests y tooling

| Comprobación | Resultado |
|---|---|
| `npx eslint .` | ✅ **limpio**, 0 errores 0 warnings |
| TypeScript en build | ✅ compila en 7,2 s sin errores |
| `tsconfig.json` `strict` | ✅ `true` |
| Uso de `any` | ✅ **ninguno** en todo `src/` |
| Script `typecheck` | ❌ no existe en `package.json` |
| Script `test` | ❌ no existe |
| Framework de tests | ❌ ninguno instalado |
| Tests | ❌ ninguno |
| `error.tsx` / `global-error.tsx` | ❌ ninguno |
| `not-found.tsx` / `loading.tsx` propios | ❌ ninguno |
| Analítica | ❌ ninguna |
| `.github/` (CI) | ❌ **no existe el directorio** |
| Lockfile | `package-lock.json` (npm). Sin `pnpm-lock.yaml` |

---

## 13. Contraste con lo que afirma el `README.md`

| Afirmación | Realidad |
|---|---|
| `README:33` «CI/CD: GitHub Actions» | **Falso.** No existe `.github/` |
| `README:172` «Lighthouse Score: 95+ (Performance)» | **Sin respaldo.** Ninguna medición en el repo. Con 302 KB de JS y el body vacío, es muy improbable |
| `README:173` «Core Web Vitals: All passing» | **Sin respaldo.** Ninguna medición |
| `README:174` «SEO: Optimized with meta tags» | **Falso.** Sólo title+description; sin OG, sitemap, robots ni JSON-LD |
| `README:175` «Accessibility: WCAG 2.1 AA compliant» | **Falso.** El formulario no tiene labels (apartado 8) |
| `README:15,26` «Animations: Framer Motion» | **Falso.** No se importa en ningún sitio |
| `README:46` «Node.js 18.17 or later» | Sin verificar; Next 16 exige Node ≥ 20 |
| `README:48` «npm or yarn» | Coherente con hoy, pendiente de actualizar tras migrar a pnpm |
| Estructura `README:80-102` | Correcta salvo que omite `components/icons/` |

---

## 14. Lo que NO he podido verificar

En honor a no suponer nada:

1. **Atribución exacta de KB por librería en el bundle de cliente.** Sin source
   maps no se puede repartir con precisión. Sólo tengo el recuento de módulos.
2. **Qué contiene exactamente el error de Resend** que se loguea en la línea 69.
   Habría que provocar un fallo real contra su API.
3. **Peso real de las imágenes optimizadas en producción.** Requiere cargar la
   página con la red instrumentada.
4. **Puntuación real de Lighthouse / Core Web Vitals.** No he ejecutado
   Lighthouse.
5. **Resultado de una auditoría automática de accesibilidad** (axe). El
   apartado 8 sale de lectura de código.
6. **Comportamiento con JS desactivado o con crawlers sin JS.** Inferido del
   HTML servido, no probado con un crawler real.
7. **Versión de Node en producción (Vercel).** No la he consultado.

---

## 15. Estado de los «Known issues» del CLAUDE.md

| # | Punto | Veredicto |
|---|---|---|
| 1 | `route.ts` loguea datos del formulario | ✅ **Confirmado** — 5 `console.log`; la línea 16 vuelca el body completo |
| 2 | Sin rate limiting ni validación de origen | ✅ **Confirmado** |
| 3 | Sin cabeceras de seguridad | ✅ **Confirmado** en código y contra producción |
| 4 | `<html lang="en">` hardcodeado | ✅ **Confirmado** — `layout.tsx:33`, nada lo actualiza |
| 5 | Demasiados componentes de cliente | ✅ **Confirmado** — 14/20; 6 lo son sólo por `useTranslation` |
| 6 | Sin tests, error boundaries ni analítica | ✅ **Confirmado** los tres |
| 7 | README afirma cosas no respaldadas | ✅ **Confirmado** — apartado 13 |

Los siete puntos siguen vigentes. Ninguno estaba ya resuelto.

### Añadidos que no estaban en la lista

1. La página se sirve sin contenido (apartado 2) — **más grave que cualquiera
   de los siete**.
2. `framer-motion` declarado y no usado, y anunciado en tres sitios.
3. `flag-icons` completo: 44 % del CSS para 2 banderas.
4. El formulario de contacto no tiene labels.
5. La API devuelve al cliente la respuesta cruda de Resend.
6. Ningún `<Image>` define `sizes`; dos imágenes de 1 MB con `priority`.
7. Código muerto: `animations.ts` y `types/index.ts` vacíos, `constants.ts` sin
   usar y con datos incorrectos, `avatar.jpg` (1,2 MB) sin referenciar.
